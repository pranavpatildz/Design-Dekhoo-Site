# DesignDekhoo - Backend

This is the backend for the DesignDekhoo application.

## Getting Started

1.  Install dependencies: `npm install`
2.  Start the server: `npm start`

The server will be running on the port specified in the `.env` file.
